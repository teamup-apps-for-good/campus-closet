<%= form_with(model: pickup) do |form| %>
  <% if pickup.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pickup.errors.count, "error") %> prohibited this pickup from being saved:</h2>

      <ul>
        <% pickup.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :donor_id, style: "display: block" %>
    <%= form.text_field :donor_id %>
  </div>

  <div>
    <%= form.label :receiver_id, style: "display: block" %>
    <%= form.text_field :receiver_id %>
  </div>

  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.text_field :item_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
